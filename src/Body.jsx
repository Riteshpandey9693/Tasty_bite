import RestaurantContainer from "./RestaurantContainer";

const Body = () => {
    return (
        <div className="px-[35px] max-w-[1400px] mx-auto">
            <RestaurantContainer />
        </div>
    );
};

export default Body;
